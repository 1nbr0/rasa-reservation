version: "3.1"

rules:
  - rule: Say goodbye anytime the user says goodbye
    steps:
      - intent: goodbye
      - action: utter_goodbye

  - rule: Go back to choice list when say no
    steps:
      - intent: denied
      - action: utter_salutation
